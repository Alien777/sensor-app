#ifndef SENSOR_SERVER_H
#define SENSOR_COSENSOR_SERVER_HNTROLLER_H
#define MIN(a, b) ((a) < (b) ? (a) : (b))

#include <esp_log.h>
#include <esp_http_server.h>

void server_initial();


#endif // SENSOR_MEMORY_H